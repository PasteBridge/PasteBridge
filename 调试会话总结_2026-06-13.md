# 调试会话总结_2026-06-13

## 状态概览

### 已完成
- **APK 构建成功**: `:androidApp:assembleDebug` 通过，APK 约 94MB
- **UniFFI Kotlin bindings 编译通过**: 修复了 PasteBridgeException 的 errorMessage 字段冲突
- **DiscoveryService.android.kt 修复**: uniffiDiscovered 类型别名改为 FQN
- **UniFFI Rust 代码统一**: 双端 (Desktop + Android) 使用同一 Rust crate
- **mDNS Rust 实现**: Desktop 和 Android 均使用 Rust mdns-sd crate
- **Emulator 可用**: Android 9 (API 28), x86
- **APK 安装成功**: adb install 成功
- **uniffi-bindgen-java-jni 安装**: 可生成 JNI (替代 JNA) 绑定

### 当前问题
APK 安装后崩溃: `java.lang.UnsatisfiedLinkError: dlopen failed: library "libc.so.6" not found`

### 根因分析
1. UniFFI -> JNA -> libjnidispatch.so
2. JNA 5.13.0 Maven Central jar 包含 linux-x86 编译的 libjnidispatch.so
3. linux-x86 .so 链接 glibc (依赖 libc.so.6)
4. Android 使用 Bionic libc, 没有 libc.so.6
5. 需要 Android-native 编译的 libjnidispatch.so

### 尝试过的方案
| 方案 | 结果 | 原因 |
|------|------|------|
| APK 注入 android-x86/libjnidispatch.so classpath 资源 | 失败 | Android PathClassLoader 不搜索 APK 根目录 |
| System.loadLibrary("jnidispatch") 预加载 | 失败 | linux-x86 .so 链接 glibc |
| jna.nolibrary=true / jna.nosys=true | 失败 | JNA 5.13.x 不支持 |
| 本地 Maven 仓库替换 JNA jar | 失败 | jar classpath 资源在 APK 中不可见 |
| uniffi-bindgen-java-jni (JNI 替代 JNA) | 部分成功 | 生成了绑定但 v0.1.0 有兼容性问题 |
| NDK 编译 JNA native 源码 | 待完善 | libffi 构建需要完整 configure |

### 下一步: NDK 编译 libjnidispatch.so
1. 用 NDK cmake 构建 libffi for Android
2. 编译 dispatch.c + callback.c 链接 libffi
3. 替换 jniLibs/x86/libjnidispatch.so
4. Rebuild APK
5. Install + verify

## 已修改文件
- MainActivity.kt - companion init { System.loadLibrary("jnidispatch") }
- shared/build.gradle.kts - JNA 5.13.0 依赖
- paste_bridge_core.kt - UniFFI 绑定
- DiscoveryService.android.kt - discovery 修复
- settings.gradle.kts - 本地 Maven repo
- jniLibs/ - JNA .so 文件
