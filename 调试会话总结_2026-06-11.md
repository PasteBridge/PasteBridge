# 调试会话总结

**日期**: 2026-06-11
**任务**: 分析 `多端同步实现.md` 并自主继续调试

---

## 一、初始状态读取

### 1.1 上次中断位置

`分析多端同步实现.md` 文件共 1360 行，结尾为 `---` 分隔符，最后操作是 `adb shell "kill"` 命令的引用，但前一次 `killall -9 nc 2>/dev/null; killall -9 sh 2>/dev/null` 反复失败。

### 1.2 文件背景

项目是 PasteBridge：跨平台剪贴板同步工具。代码结构:
- `crates/core/` - 核心 Rust 库（API、discovery、state、sync_device）
- `crates/desktop/` - Slint 桌面 UI
- `Android app` - Kotlin + UniFFI
- `examples/` - 测试 smoke 程序

关键 API 路径: `GET /clipboard/history`, `POST /clipboard/copy`

---

## 二、网络拓扑重新诊断

### 2.1 手机状态变化

| 时段 | 手机 IP | 网络 |
|------|---------|------|
| 上次 | `192.168.5.130/24` | 普通 WiFi |
| **现在** | `10.239.38.89/24` | **WiFi 热点**（手机做 AP） |

PC 端对应变化:
```
          0.0.0.0          0.0.0.0     10.239.38.89     10.239.38.30     45
```
PC 通过 `10.239.38.30` 直连 `10.239.38.89`（手机热点网关）。**同子网 10.239.38.0/24，无 NAT**。

### 2.2 关键决策

由于手机现在开热点充当网关，**与 PC 处于同一子网**——这是修复所有"扫不到 peer"问题的前提。之前 `192.168.5.x` 跨子网有 Mihomo TUN 劫持；现在 `10.239.38.x` 子网内 PC → phone 是直连。

---

## 三、环境清理

### 3.1 手机端进程清理

**问题**: 之前 18792 上有多个僵尸 nc 进程（user `u0_a336` 权限不可杀），bind "Address already in use" 持续。

**解决**: 让旧进程自然死亡（kill 因权限被拒），等 18792 释放后启新 server。

### 3.2 桌面 app 状态

- `paste_bridge_desktop.exe` (PID 62296) 持续监听 18792 + 28792
- mDNS 在 5353 UDP 上 announce
- Mihomo TUN (198.18.0.1) 正在向 `10.239.38.120:18792` 主动连接（fake-IP 探测，与本测试无关）

---

## 四、新 HTTP Server 设计

### 4.1 文件结构

```
PasteBridge/scripts/
├── pb_dummy_server.sh   # 主入口: 启动 nc 监听 + 维护 while 循环
└── (无 handler - 改用 cat 流)
```

### 4.2 JSON 格式修复

**第一次 bug**: Mock 数据用了 `content_type: "text/plain"`, Rust 反序列化失败：
```
unknown variant `text/plain`, expected `text` or `image`
```

**修复**: 改为 `content_type: "text"`，并补全所有字段（`content_hash`, `created_at` 等）：
```json
[{"id":99,"content_type":"text","content_text":"HELLO-FROM-ANDROID",
  "content_hash":"abc99def","mime_type":null,"file_size":null,
  "width":null,"height":null,"source_ip":null,
  "created_at":1700000000000,"is_favorite":false},
 {"id":100,"content_type":"text","content_text":"test-from-phone-2",...}]
```

### 4.3 启动 server

```sh
adb push pb_dummy_server.sh → /data/local/tmp/
chmod +x
nohup sh /data/local/tmp/pb_dummy_server.sh 18792 > /dev/null 2>&1 &
```

**验证**: `curl http://10.239.38.89:18792/clipboard/history` 返回 2 条 mock data ✅

---

## 五、编译错误修复

### 5.1 sync_device.rs 类型不匹配

**错误** (2 处):
```
error[E0308]: mismatched types
   --> crates/core/src/sync_device.rs:151:9
    |
    |         pushed,
    |         ^^^^^^ expected `u64`, found `usize`
```

**修复**: `crates/core/src/sync_device.rs:151` 和 `:248`，两处 `pushed` 改成 `pushed as u64`。

### 5.2 编译新 example

写了 3 个新 example:
- `tcp_probe.rs` - 单点 TCP/HTTP 探针
- `tcp_probe_many.rs` - 并发子网扫描
- `phone_sync_smoke.rs` - 直连 sync_with_device

---

## 六、关键验证

### 6.1 Rust TCP/HTTP 探针 ✅

```sh
target/debug/examples/tcp_probe.exe 10.239.38.89:18792
# [probe] target=10.239.38.89:18792 GET /clipboard/history
# [probe] TCP connect OK (Ok(10.239.38.30:5679))
# [probe] sent 80 bytes
# [probe] read 370 bytes
# HTTP/1.1 200 OK ... [{"id":99,"content_text":"HELLO-FROM-ANDROID",...}]
```

### 6.2 整子网并发扫描 ✅

```sh
target/debug/examples/tcp_probe_many.exe 10.239.38 18792 64
# [scan] subnet=10.239.38.0/24 port=18792 cand=254 conc=64
# [scan] HIT 10.239.38.89:18792 -> HTTP/1.1 200 OK ; body[:120]=[{"id":99,"content_text":"HELLO-FROM-ANDROID",...
# [scan] HIT 10.239.38.30:18792 -> HTTP/1.1 200 OK ; body[:120]=2000...
# [scan] done in 1.6643701s, found 2 hits
```

**关键确认**: PC 能通过 10.239.38.89 直连 phone (1.66s 扫完)。

### 6.3 端到端 sync 闭环 ✅ 🎉

```sh
target/debug/examples/phone_sync_smoke.exe 10.239.38.89:18792
# [phone-smoke] local state count = 2
# [phone-smoke] calling sync_with_device against 10.239.38.89:18792
# [phone-smoke] report: peer=direct-10.239.38.89:18792 pulled=2 added=2 pushed=1 err=None
# [phone-smoke] final local state count = 4
# [phone-smoke] #0: id=4 text=Some("test-from-phone-2")
# [phone-smoke] #1: id=3 text=Some("HELLO-FROM-ANDROID")
# [phone-smoke] #2: id=2 text=Some("client-seed-2")
# [phone-smoke] #3: id=1 text=Some("client-seed-1")
```

**Phone 端日志确认**:
```
[23:27:51] REQ: GET /clipboard/history HTTP/1.1||User-Agent: ureq/2.12.1
[23:27:52] REQ: POST /clipboard/copy HTTP/1.1||Content-Length: 13||||client-seed-2
```

**Pull + Push 完整工作**:
- 2 条从 phone 拉到本地
- 1 条从本地 push 到 phone (latest local text)
- err=None

---

## 七、遗留问题

### 7.1 unicast_full_scan 在 Mihomo TUN 下失败

`unicast_full_scan.rs` 用 `Discovery` 全套机制（mDNS + unicast），8 秒超时后未发现 10.239.38.89:
```
[unicast] verify http://10.239.38.89:18792/clipboard/history -> err ... (os error 10060)
```

`phone_sync_smoke` 用同一 ureq 库**能成功**——区别在于并发度（unicast probe 32 线程，phone_sync_smoke 单 socket）。

**推测原因**: Mihomo TUN (198.18.0.1, metric 0) 在高并发下劫持 10.239.38.89 的 TCP 握手，导致 10060 timeout。

### 7.2 32 并发下 phone 单连接 nc 失败

即使 Mihomo 不劫持，phone 的 `nc -l` 是 **one-shot**（一次 accept 一个连接后退出）。32 线程并发触发:
- 第 1 个 accept 成功 → 处理 → 退出
- 2-32 个 SYN 在 kernel accept queue 排队
- 队列满后新 SYN 被 RST 或丢弃

`tcp_probe_many` 用 64 线程但 254 IP **分散**到每线程 4 个，竞争温和，所以工作。
`unicast_full_scan` 触发同一 phone 多次，竞争激烈，所以失败。

### 7.3 真实修复方向

- **生产端**: Android PasteBridge app 用 `tiny_http` 多线程 accept — 真实 app 不会有此问题
- **测试端**: 改成单线程 unicast，或多个 phone 实例分散压力
- **路由端**: 给 10.239.38.0/24 加 source-based 路由绕过 Mihomo TUN

---

## 八、产出文件清单

### 8.1 新增文件

| 路径 | 用途 |
|------|------|
| `crates/core/examples/tcp_probe.rs` | 单点 TCP/HTTP 探针 |
| `crates/core/examples/tcp_probe_many.rs` | 并发子网扫描 |
| `crates/core/examples/phone_sync_smoke.rs` | 直连 phone sync 测试 |
| `crates/core/examples/unicast_full_scan.rs` | 全 discovery 路径测试 |
| `scripts/pb_dummy_server.sh` | Phone 端 HTTP server |
| `scripts/pb_handler.sh` | (已弃用, 改用 cat 流) |
| `scripts/find_inode.sh` | 通过 inode 找进程 |
| `scripts/find_18792.sh` | (调试用, 已弃用) |

### 8.2 修改文件

| 路径 | 修改内容 |
|------|---------|
| `crates/core/src/sync_device.rs` | 2 处 `pushed as u64` |

### 8.3 日志/调试文件

| 路径 | 用途 |
|------|------|
| `client.err.log` | unicast_smoke client 输出 |
| `client.out.log` | unicast_smoke stdout |
| `scan_stderr.log` / `scan2.log` / `scan_full.log` / `scan_after_sync.log` | 各扫描结果 |
| `server.err.log` / `server.out.log` | API server 调试 |
| `debug-logcat.txt` | Android logcat 抓取 |

---

## 九、关键命令速查

```sh
# 启 phone HTTP server
adb -s 85e3eaa9 shell "nohup sh /data/local/tmp/pb_dummy_server.sh 18792 > /dev/null 2>&1 &"

# 测 phone 直连
curl -s -m 5 "http://10.239.38.89:18792/clipboard/history"

# Rust TCP 探针
target/debug/examples/tcp_probe.exe 10.239.38.89:18792

# 整子网扫描
target/debug/examples/tcp_probe_many.exe 10.239.38 18792 64

# 端到端 sync
target/debug/examples/phone_sync_smoke.exe 10.239.38.89:18792

# 全 discovery 扫描
target/debug/examples/unicast_full_scan.exe

# adb reverse (备用)
adb -s 85e3eaa9 reverse tcp:18792 tcp:18792
